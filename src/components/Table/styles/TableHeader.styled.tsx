import styled from "styled-components"

export const TableHead = styled.thead`
    .filter-selection{
        float: right;
        display:flex;
        flex-direction: column;
        &:hover{
            cursor: pointer;
        }
    }

    .table-head-col{
        float: left;
    }

    
`